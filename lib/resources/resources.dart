import 'strings/strings.dart';
import 'values/values.dart';
import 'drawables/drawables.dart';

class Res {
  static Strings string = Strings();
  static AppColors colors = AppColors();
  static AppTranslations appTranslations = AppTranslations();
  static Drawables drawable = Drawables();
  static AppTheme appTheme = AppTheme();
}
